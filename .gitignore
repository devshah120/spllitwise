node_modules/
.env
*.log
.DS_Store
coverage/
dist/

# Firebase service-account key (push notifications) — a secret, never committed.
src/config/firebase-service-account.json

# User-uploaded files
uploads/
preview/
